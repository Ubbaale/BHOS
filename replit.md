# Carehub - Healthcare Staffing Platform

## Overview
Carehub is a comprehensive platform designed to streamline healthcare staffing and Non-Emergency Medical Transportation (NEMT) services. It features a landing page and job management system that connects healthcare facilities with qualified medical professionals. Additionally, it incorporates a robust NEMT marketplace where patients can book rides and drivers can manage requests, complete with real-time tracking, in-app communication, and integrated payment processing. The platform aims to enhance efficiency, reliability, and patient care in both healthcare staffing and medical transportation.

## User Preferences
Preferred communication style: Simple, everyday language.

## System Architecture

### Core Technologies
The platform is built as a monorepo, separating client, server, and shared code.
-   **Frontend**: React 18 with TypeScript, Wouter for routing, TanStack React Query for state management, Tailwind CSS with shadcn/ui for UI components, and Vite for efficient builds.
-   **Backend**: Node.js with Express, TypeScript (ESM), providing a RESTful JSON API.
-   **Database**: PostgreSQL, managed with Drizzle ORM.

### Key Features
-   **Healthcare Staffing**: Features an interactive job map (Leaflet.js) for job postings and real-time updates via WebSockets. User roles include employer, healthcare worker, patient, and driver, all capable of posting jobs post-authentication.
-   **Issue Reporting**: A support ticket system with file attachment capabilities and email notifications.
-   **Medical Ride-Hailing (NEMT)**: Operates on a marketplace model allowing patients to post requests for drivers to claim. Includes a comprehensive ride workflow with various statuses, real-time updates, in-app chat, and navigation integration. Advanced features include wait time tracking with charges, vehicle type matching, distance-based fare calculation, and safety features like trip sharing and an SOS button. The system also manages policies such as cancellation windows, surge pricing, and provides patient email notifications for booked rides. Supports medical notes for drivers, round trips, recurring rides, ride history, and post-ride ratings. Drivers benefit from a live tracking map, an enhanced driver card, and a surge zone map indicating demand.
-   **Toll Zone Estimation**: 25 major US toll zones seeded on startup (bridges, tunnels, turnpikes). Route-based toll estimation via `POST /api/toll-zones/estimate` checks which toll zones fall along the straight-line route. Estimated tolls are shown in fare breakdowns during booking (book-ride, caregiver-book-ride, facility-book-ride) and saved with each ride. Drivers confirm actual tolls at trip completion. Tolls appear in trip history and tax documents as deductible expenses.
-   **Caregiver/Family Portal**: A dedicated portal for managing rides for loved ones, including adding/editing patient profiles, quick ride booking for managed patients, and summary statistics.
-   **Facility Discharge Coordination**: A portal for hospital and clinic staff to book patient transport, with preset transport options and integration with facility-specific addresses.
-   **Driver Management**: Encompasses self-service onboarding, admin review with KYC verification, earnings dashboards (including weekly summaries and trip history), driver statistics, and alerts for expiring documents. Compliance enforcement blocks non-compliant drivers from accepting rides.
-   **Payment System**: Utilizes Stripe for upfront payments, tip collection, and supports insurance billing. Integrates Stripe Connect for driver payouts, offering instant and standard payout options.
-   **Native Mobile App UI**: Designed for iOS and Android with a platform detection hook, bottom tab bar navigation, compact headers, quick action tiles, and safe area inset support. The mobile experience is touch-optimized and PWA-enhanced.
-   **Mobile App Integration**: Provides a JWT token-based API (v2.0) for native apps, covering authentication, job/ride management, driver features, patient profiles, surge pricing, and payments. Includes Firebase push notification integration. All mobile API responses use an Uber-style envelope format `{ status, data, meta: { timestamp, version } }` with structured error codes. Push notifications are rich and context-aware, including driver info, vehicle details, ETA, fare, and actionable buttons (Track, Contact, Rate, Receipt).
-   **Legal Protection and Compliance**: Implements Terms of Service and Privacy Policy pages, requires digital signing of an Independent Contractor Agreement with server-side content hashing, and includes an audit logging system for sensitive actions. Driver compliance is enforced based on license, insurance, inspection, and background check statuses.
-   **Security and Data Protection**: Features PostgreSQL-backed session security, rate limiting for authentication, bcrypt password hashing, Zod schema validation for API inputs, comprehensive security headers, global rate limiting, and secure file uploads with MIME type restrictions. WebSocket connections are secured with single-use authentication tokens.
-   **IT Services Dispatch & Ticketing**: A FieldNation-style system for healthcare IT dispatch. Users submit tickets with dispatch details (scheduled date/time, site address, on-site contact, equipment needed, special instructions, pay rate/type). Ticket categories include network, hardware, software, printer, EHR system, security, phone system, email, and backup. Dashboard shows ticket stats with filtering by status. FieldNation-grade features include: tech ETA tracking (en_route/arriving/on_site), check-in/check-out with GPS verification and time tracking, deliverables/proof of work uploads, two-way ratings (customer rates tech, tech rates customer), work order templates (save/load), talent pools for targeted routing, and routing modes (broadcast/talent_pool/direct_assign). Ticket owners see full job lifecycle: ETA status, check-in/out times, payment breakdown, proof of work, and rating forms. All routes under `/api/it/*`. Frontend at `/it-services`. DB tables: `it_companies`, `it_service_tickets`, `it_ticket_notes`, `it_talent_pools`, `it_talent_pool_members`, `it_work_order_templates`.
-   **IT Services Payment System**: Escrow-based payment with tiered platform fees. Payment terms: Instant (15% fee), Net 7 (12%), Net 14 (10%), Net 30 (8%). Companies fund escrow via Stripe when posting jobs; funds are held until work is approved. Budget caps for hourly jobs limit max spend; overtime rates for fixed-price jobs cover work beyond estimated duration. Overage detection at checkout flags extra time for company approval. Payment history dashboards for both companies and techs show earnings, pending payouts, and payout dates. Routes: `/api/it/tickets/:id/fund-escrow`, `/api/it/tickets/:id/confirm-escrow`, `/api/it/tickets/:id/approve-overage`, `/api/it/tickets/:id/reject-overage`, `/api/it/payment-terms`, `/api/it/company/payment-history`, `/api/it/tech/payment-history`. Schema fields: `paymentTerms`, `platformFeePercent`, `escrowAmount`, `stripePaymentIntentId`, `escrowStatus`, `budgetCap`, `overtimeRate`, `overageApproved`, `overageAmount`, `overageHours`, `payoutDate`, `overtimeAlertSent`.
-   **IT Tech Onboarding & Dispatch**: Technicians apply at `/it-tech/apply` (public, no auth required) creating a user account with `it_tech` role and `it_tech_profiles` record. Applications go through admin approval (`/api/it/admin/techs/:id/approve|reject`). Approved techs access a dashboard at `/it-tech` with tabs: Available jobs (filtered by routing mode/talent pool membership), Active jobs (with ETA, check-in/out, deliverables, complete workflow), Completed jobs (with ratings), Earnings (total earnings, pending payouts, timeliness score, performance stats, 1099-NEC tax forms), and Settings (IC Agreement signing, W-9 tax info, certification uploads). Tech profiles track: averageRating, totalEarnings, timelinessScore, onTimeCheckIns, lateCheckIns, accountStatus, complaintCount, verifiedComplaintCount. Routes: `/api/it/tech/*` for tech actions, `/api/it/admin/techs/*` for admin management.
-   **IT Tech Contractor Onboarding**: SSN (last 4), tax classification, business name, W-9 address info collection. Digital signing of Independent Contractor Agreement with IP/userAgent logging to `itTechContractorAgreements` table. 1099-NEC tax data generation per year with $600 threshold tracking via `itTechAnnualEarnings` table. Certification document uploads (PDF/image) stored to `uploads/it-certs/` with admin verification. Routes: `/api/it/tech/contractor-onboarding`, `/api/it/tech/sign-ic-agreement`, `/api/it/tech/tax-years`, `/api/it/tech/1099/:year`, `/api/it/tech/certifications/upload`.
-   **IT Tech Complaint & Enforcement System**: Companies can file formal complaints against techs tied to specific tickets (categories: time_padding, no_show, poor_work, unprofessional, damage, safety_violation, misrepresentation, other). Auto-hold after 3 complaints blocks tech from accepting new tickets until admin review. Admin panel shows all complaints with review options (verified/dismissed/investigating) and enforcement actions (warn/suspend/ban/reinstate/remove_hold). Suspension supports timed or indefinite periods. Banned techs are permanently deactivated. Tech dashboard shows account status banner. Enforcement log tracks all admin actions. DB tables: `it_tech_complaints`, `it_tech_enforcement_log`. Routes: `/api/it/tech/:techUserId/report`, `/api/it/admin/complaints`, `/api/it/admin/complaints/:id/review`, `/api/it/admin/techs/:id/enforce`, `/api/it/admin/techs/:id/enforcement-history`, `/api/it/tech/account-status`.
-   **IT Dispute Mediation**: Either party (company or tech) can request mediation on a ticket. Admins resolve disputes with outcomes: favor_company, favor_tech, split, or cancel. Split resolution halves the payout. Routes: `/api/it/tickets/:id/request-mediation`, `/api/it/admin/tickets/:id/mediate`.
-   **Admin Dashboard**: Comprehensive admin panel at `/admin` with a proper sidebar navigation and permission-gated sections. Features include: full ride detail view with fare breakdown/route/timeline, driver detail view showing all onboarding fields (KYC, documents, vehicle, contractor info), earnings dashboard with financial breakdowns (fares, revenue, driver payouts, tips, tolls, refunds, monthly breakdown, payment status), account creation for any user type (user/patient/driver/employer/admin/IT company/IT tech), email verification, role management, account blocking/unblocking for all user types, and ride cancellation/refund processing. Supports **custom admin permissions** — admins can create sub-admin accounts with preset access levels (Finance, Dispatcher, Support, Accounts Manager) or custom permission sets. Permissions are enforced both in the UI (sidebar/sections hide) and server-side (`requirePermission` middleware on all admin API routes). Available permissions: dashboard, rides, drivers, patients, earnings, accounts, incidents, it_services, dispatch. Empty permissions array = full access. API endpoints: `/api/admin/earnings`, `/api/admin/users`, `/api/admin/create-account`, `/api/admin/users/:id/role`, `/api/admin/users/:id/verify-email`, `/api/admin/users/:id/permissions`.
-   **Web Authentication**: A single unified `/login` page for all user types (admin, driver, patient). Smart mobile detection routes phone users to the Flutter app and desktop users to the web login. Login credentials determine the destination: admins go to `/admin`, drivers to `/driver`, patients to `/`. The `/driver/login` route also points to this same unified page. Includes a multi-step forgot password flow at `/forgot-password` with 5-digit email codes via SendGrid. The Header shows "Sign In" for unauthenticated users and role-appropriate account links when logged in. All protected routes redirect to `/login` when not authenticated.

## External Dependencies

-   **Database**: PostgreSQL
-   **Mapping**: Leaflet.js, Google Places Autocomplete
-   **UI/UX Libraries**: Radix UI, Tailwind CSS, Class Variance Authority, Lucide React, react-icons
-   **Form Management**: React Hook Form, Zod
-   **Date Utilities**: date-fns
-   **Payment Processing**: Stripe
-   **Email Services**: SendGrid
-   **Development Tooling**: TypeScript, Vite, PostCSS, esbuild